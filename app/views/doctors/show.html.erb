<h2><%= @doctor.name %></h2>
<h3>Appointments:</h3>
<% @doctor.appointments.each do |a| %>
<ul>
  <li><%= a.appointment_datetime.strftime("%B %e, %Y at %H:%M") %> 
  with <%= link_to a.patient.name, patient_path(a.patient)  %>
  </li>
</ul>
<% end %>