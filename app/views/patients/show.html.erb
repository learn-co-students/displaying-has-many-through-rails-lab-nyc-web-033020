<h2>Patient: <%= @patient.name %></h2>

<% @patient.appointments.each do |a| %>
<ul>
  <li>
    <%= a.appointment_datetime.strftime("%B %e, %Y at %H:%M") %>
    with <%=link_to a.doctor.name, doctor_path(a.doctor)%> 
  </li>
</ul>
<% end %>